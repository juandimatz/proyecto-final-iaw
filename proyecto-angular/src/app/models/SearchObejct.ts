export class SearchObject {
    
    public brand;
    public minPrice;
    public maxPrice;
    
    constructor(brand, minPrice, maxPrice) {
        this.brand = brand;
        this.minPrice = minPrice;
        this.maxPrice = maxPrice;
    }

}